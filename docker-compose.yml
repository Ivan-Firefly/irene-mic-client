version: '3'
services:
  voice-assistant:
    container_name: irene-mic-client
    image: firefly27/irene-mic-client:arm
    devices:
      - /dev/snd:/dev/snd
    volumes:
      - ./options.json:/options.json
    restart: 'unless-stopped'
